# edu-ai-sales-s3 — cohort working notes

This is the **course** repo. Your own sales system lives at `~/sales/` on your machine, copied
from `sales-starter/`. Never commit your own customer data here.

- `sales-starter/CLAUDE.md` is the contract. Read it before anything else.
- `sample-case/` is ServiceGrid — use it when you cannot use your own data.
- One folder per session, added as we go.

Course runs on **Claude Code and Codex**. Either is enough; both read `CLAUDE.md` / `AGENTS.md`.
