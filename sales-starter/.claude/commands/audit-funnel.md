---
description: Audit your funnel numbers, then answer an inbound lead using what the audit found
---

Follow `.claude/skills/audit-funnel/SKILL.md` in this repo, start to finish.

If the user passed anything after the command, treat it as the input — a path to a CSV, six
numbers typed inline, or the text of an inbound message: $ARGUMENTS

Read `CLAUDE.md` first — it is the contract, and it says where findings must be filed.
